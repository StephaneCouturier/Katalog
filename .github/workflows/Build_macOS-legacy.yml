name: Build macOS Legacy (v2.5 Qt only)
#v1 - Force v2.5 checkout (pure Qt6, no KDF dependencies)
on:
  # Allow manual triggering from GitHub web interface
  workflow_dispatch:
    inputs:
      version_tag:
        description: 'Version tag (e.g., v2.5-macos)'
        required: false
        default: 'v2.5-macos-build'

  # Trigger on push to the macos-legacy-build branch only
  push:
    branches: [ macos-legacy-build ]
    paths: 
      - '.github/workflows/macos-legacy-build.yml'  # Only when workflow changes

jobs:
  build-macos-legacy:
    runs-on: macos-14  # macOS Sonoma (Apple Silicon)

    steps:
    - name: Checkout v2.5 codebase (force)
      uses: actions/checkout@v4
      with:
        # Force checkout v2.5 tag (pure Qt, no KF dependencies)
        ref: v2.5
        submodules: recursive

    - name: Verify v2.5 checkout
      run: |
        echo "Current commit: $(git rev-parse HEAD)"
        echo "Current tag: $(git describe --tags --exact-match 2>/dev/null || echo 'not on a tag')"
        
        # Show we're on v2.5
        git log --oneline -n 3
        
        echo "=== Verifying this is pure Qt code (no KF dependencies) ==="
        
        # Check CMakeLists.txt for KF dependencies (should not exist in v2.5)
        if grep -q "find_package.*KF[56]" CMakeLists.txt 2>/dev/null; then
          echo "❌ ERROR: Found KF dependencies in CMakeLists.txt - not pure v2.5 code!"
          grep "find_package.*KF" CMakeLists.txt
          exit 1
        else
          echo "✅ Good: No KF dependencies found in CMakeLists.txt"
        fi
        
        # Check for .pro file (v2.5 should have this)
        if [ -f "Katalog.pro" ]; then
          echo "✅ Found Katalog.pro file (v2.5 indicator)"
        else
          echo "⚠️  No .pro file found"
        fi

    - name: Install dependencies via Homebrew
      run: |
        # Show what's already installed to help debug conflicts
        echo "=== Pre-installed tools ==="
        echo "cmake: $(command -v cmake && cmake --version | head -1 || echo 'not found')"
        echo "ninja: $(command -v ninja && ninja --version || echo 'not found')"
        echo "qmake: $(command -v qmake && qmake --version | head -1 || echo 'not found')"
        echo ""
        
        # Update Homebrew
        brew update

        # Handle cmake installation (may be pre-installed from different tap)
        if command -v cmake >/dev/null 2>&1; then
          echo "cmake already installed: $(cmake --version)"
          CMAKE_INSTALLED=true
        else
          CMAKE_INSTALLED=false
        fi

        if [ "$CMAKE_INSTALLED" = "false" ]; then
          brew install cmake
        else
          echo "Using existing cmake installation"
        fi

        # Install ninja (less likely to have conflicts)
        if ! command -v ninja >/dev/null 2>&1; then
          brew install ninja
        else
          echo "ninja already installed: $(ninja --version)"
        fi

        # Install Qt6 (back to Qt6, but without KF dependencies)
        if brew list qt@6 >/dev/null 2>&1; then
          echo "Qt6 already installed"
        else
          echo "Installing Qt6..."
          brew install qt@6
        fi

        # Install imagemagick for icon conversion
        if ! command -v convert >/dev/null 2>&1; then
          echo "Installing ImageMagick for icon conversion..."
          brew install imagemagick
        else
          echo "ImageMagick already available"
        fi

        echo "Skipping KDE frameworks - using pure Qt6 build from v2.5 codebase"
        echo "This version doesn't require KF5/KF6 dependencies"

    - name: Set Qt6 environment
      run: |
        # Set up Qt6 paths
        echo "$(brew --prefix qt@6)/bin" >> $GITHUB_PATH
        echo "CMAKE_PREFIX_PATH=$(brew --prefix qt@6)" >> $GITHUB_ENV
        echo "Qt6_DIR=$(brew --prefix qt@6)/lib/cmake/Qt6" >> $GITHUB_ENV

        # Display Qt6 version info
        echo "Qt6 installation path: $(brew --prefix qt@6)"
        echo "Qt6 version: $($(brew --prefix qt@6)/bin/qmake -version)"
        
        # List available Qt6 libraries
        echo "Available Qt6 libraries:"
        ls -la "$(brew --prefix qt@6)/lib" | grep -E "Qt6.*\.framework|libQt6.*\.dylib" | head -10

    - name: Set version information
      run: |
        if [ "${{ github.event_name }}" = "workflow_dispatch" ] && [ -n "${{ github.event.inputs.version_tag }}" ]; then
          echo "VERSION_TAG=${{ github.event.inputs.version_tag }}" >> $GITHUB_ENV
          echo "IS_RELEASE=false" >> $GITHUB_ENV
        else
          echo "VERSION_TAG=v2.5-macos-$(date +%Y%m%d-%H%M)" >> $GITHUB_ENV
          echo "IS_RELEASE=false" >> $GITHUB_ENV
        fi

        echo "Building version: $VERSION_TAG"

    - name: Build Katalog v2.5 (pure Qt6)
      run: |
        mkdir build
        cd build

        # Set Qt6 environment  
        export CMAKE_PREFIX_PATH="$(brew --prefix qt@6)"

        # Check what build system v2.5 uses
        echo "=== Checking available build systems ==="
        if [ -f "../Katalog.pro" ]; then
          echo "✅ Found Katalog.pro file"
        else
          echo "❌ No .pro file found"
        fi
        
        if [ -f "../CMakeLists.txt" ]; then
          echo "✅ Found CMakeLists.txt file"
          echo "First 10 lines of CMakeLists.txt:"
          head -10 ../CMakeLists.txt
        else
          echo "❌ No CMakeLists.txt file found"
        fi

        # Try build systems in order of preference for v2.5
        BUILD_SUCCESS=false
        
        if [ -f "../Katalog.pro" ]; then
          echo "=== Attempting qmake build (v2.5 .pro file found) ==="
          
          # Build with qmake (traditional Qt way)
          if qmake ../Katalog.pro -spec macx-clang CONFIG+=release; then
            echo "qmake configuration successful"
            if make -j$(sysctl -n hw.ncpu); then
              echo "✅ qmake build successful"
              BUILD_SUCCESS=true
            else
              echo "❌ qmake build failed"
            fi
          else
            echo "❌ qmake configuration failed"
          fi
          
        elif [ -f "../CMakeLists.txt" ]; then
          echo "=== Attempting cmake build (CMakeLists.txt found) ==="
          
          # Build with cmake (should work for pure Qt6 v2.5)
          if cmake .. \
            -GNinja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_PREFIX_PATH="$CMAKE_PREFIX_PATH" \
            -DCMAKE_OSX_DEPLOYMENT_TARGET=11.0 \
            -DBUILD_TESTS=OFF \
            -DCMAKE_INSTALL_PREFIX=/Applications; then
            
            echo "cmake configuration successful"
            if ninja; then
              echo "✅ cmake build successful"
              BUILD_SUCCESS=true
            else
              echo "❌ cmake build failed"
            fi
          else
            echo "❌ cmake configuration failed"
          fi
        else
          echo "❌ No build file found (.pro or CMakeLists.txt)"
        fi

        if [ "$BUILD_SUCCESS" = "false" ]; then
          echo "❌ All build attempts failed"
          exit 1
        fi

        # Check what was built
        echo "=== Build output ==="
        ls -la | grep -i katalog || echo "No Katalog executable found in build dir"
        find . -name "*.app" -o -name "Katalog*" -type f | head -10
        
        # Look for app bundle or executable
        if [ -d "Katalog.app" ]; then
          echo "✅ macOS app bundle created successfully"
          ls -la Katalog.app/Contents/ || echo "Could not list bundle contents"
        elif [ -f "Katalog" ]; then
          echo "✅ Katalog executable created, will need to create bundle manually"
        else
          echo "⚠️ No obvious Katalog app or executable found, checking all files:"
          find . -type f -name "*atalog*" -o -name "*App*" | head -20
        fi

    - name: Create app bundle structure and fix dependencies
      run: |
        cd build
        
        # Ensure we have the app bundle
        if [ ! -d "Katalog.app" ]; then
          echo "Creating app bundle manually..."
          mkdir -p Katalog.app/Contents/{MacOS,Resources,Frameworks}
          
          # Create Info.plist
          cat > Katalog.app/Contents/Info.plist << EOF
        <?xml version="1.0" encoding="UTF-8"?>
        <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
        <plist version="1.0">
        <dict>
          <key>CFBundleExecutable</key>
          <string>Katalog</string>
          <key>CFBundleIdentifier</key>
          <string>io.github.stephanecouturier.katalog</string>
          <key>CFBundleName</key>
          <string>Katalog</string>
          <key>CFBundleDisplayName</key>
          <string>Katalog</string>
          <key>CFBundleVersion</key>
          <string>${{ env.VERSION_TAG }}</string>
          <key>CFBundlePackageType</key>
          <string>APPL</string>
          <key>CFBundleSignature</key>
          <string>KTLG</string>
          <key>LSMinimumSystemVersion</key>
          <string>11.0</string>
          <key>NSHighResolutionCapable</key>
          <true/>
          <key>CFBundleIconFile</key>
          <string>Katalog.icns</string>
        </dict>
        </plist>
        EOF

          # Copy executable if it exists
          if [ -f "Katalog" ]; then
            cp Katalog Katalog.app/Contents/MacOS/
          fi
        fi

        # Use macdeployqt to handle Qt6 dependencies
        echo "Running macdeployqt to bundle Qt6 libraries..."
        MACDEPLOYQT="$(brew --prefix qt@6)/bin/macdeployqt"
        
        if [ -f "$MACDEPLOYQT" ]; then
          "$MACDEPLOYQT" Katalog.app -verbose=2 || echo "macdeployqt completed with warnings"
        else
          echo "macdeployqt not found, trying alternative approach..."
          # Manual dependency bundling would go here if needed
        fi

        # Create icon if needed
        if [ ! -f "Katalog.app/Contents/Resources/Katalog.icns" ]; then
          if [ -f "../assets/Katalog_logo_64.ico" ]; then
            echo "Converting icon..."
            # Create iconset directory
            mkdir -p Katalog.iconset
            
            # Use 'magick' instead of deprecated 'convert'
            if command -v magick >/dev/null; then
              echo "Using ImageMagick 7 (magick command)"
              magick "../assets/Katalog_logo_64.ico" -resize 512x512 "Katalog.iconset/icon_512x512.png" || echo "512x512 conversion failed"
              magick "../assets/Katalog_logo_64.ico" -resize 256x256 "Katalog.iconset/icon_256x256.png" || echo "256x256 conversion failed"
              magick "../assets/Katalog_logo_64.ico" -resize 128x128 "Katalog.iconset/icon_128x128.png" || echo "128x128 conversion failed"
              magick "../assets/Katalog_logo_64.ico" -resize 64x64 "Katalog.iconset/icon_64x64.png" || echo "64x64 conversion failed"
              magick "../assets/Katalog_logo_64.ico" -resize 32x32 "Katalog.iconset/icon_32x32.png" || echo "32x32 conversion failed"
              magick "../assets/Katalog_logo_64.ico" -resize 16x16 "Katalog.iconset/icon_16x16.png" || echo "16x16 conversion failed"
            elif command -v convert >/dev/null; then
              echo "Using ImageMagick 6 (convert command)"
              convert "../assets/Katalog_logo_64.ico" -resize 512x512 "Katalog.iconset/icon_512x512.png" || echo "512x512 conversion failed"
              convert "../assets/Katalog_logo_64.ico" -resize 256x256 "Katalog.iconset/icon_256x256.png" || echo "256x256 conversion failed"
              convert "../assets/Katalog_logo_64.ico" -resize 128x128 "Katalog.iconset/icon_128x128.png" || echo "128x128 conversion failed"
              convert "../assets/Katalog_logo_64.ico" -resize 64x64 "Katalog.iconset/icon_64x64.png" || echo "64x64 conversion failed"
              convert "../assets/Katalog_logo_64.ico" -resize 32x32 "Katalog.iconset/icon_32x32.png" || echo "32x32 conversion failed"
              convert "../assets/Katalog_logo_64.ico" -resize 16x16 "Katalog.iconset/icon_16x16.png" || echo "16x16 conversion failed"
            else
              echo "No ImageMagick found, skipping icon conversion"
            fi
            
            # Create ICNS file (make this non-fatal)
            if ls Katalog.iconset/*.png 1> /dev/null 2>&1; then
              echo "Creating ICNS file..."
              iconutil -c icns Katalog.iconset -o Katalog.app/Contents/Resources/Katalog.icns || {
                echo "⚠️ ICNS creation failed, but continuing build..."
                echo "App will work without custom icon"
              }
            else
              echo "⚠️ No PNG icons created, skipping ICNS creation"
            fi
          else
            echo "⚠️ No source icon file found, app will use default icon"
          fi
        else
          echo "✅ Icon already exists"
        fi

        # Display final bundle structure
        echo "Final app bundle structure:"
        find Katalog.app -type f | head -20

    - name: Create DMG package
      run: |
        cd build
        
        # Install create-dmg if available
        brew install create-dmg || echo "create-dmg not available, using manual method"
        
        DMG_NAME="Katalog-${{ env.VERSION_TAG }}-macOS.dmg"
        
        if command -v create-dmg >/dev/null; then
          echo "Creating DMG with create-dmg..."
          create-dmg \
            --volname "Katalog" \
            --window-pos 200 120 \
            --window-size 600 400 \
            --icon-size 100 \
            --icon "Katalog.app" 175 120 \
            --hide-extension "Katalog.app" \
            --app-drop-link 425 120 \
            "$DMG_NAME" \
            Katalog.app
        else
          echo "Creating simple DMG..."
          # Create a simple DMG
          hdiutil create -srcfolder Katalog.app -format UDZO -o "$DMG_NAME"
        fi
        
        echo "DMG created: $DMG_NAME"
        ls -lh *.dmg

    - name: Test app bundle
      run: |
        cd build
        
        echo "Testing app bundle..."
        
        # Check bundle structure
        if [ -d "Katalog.app" ]; then
          echo "✅ App bundle exists"
          
          # Check if executable exists and has correct permissions
          if [ -x "Katalog.app/Contents/MacOS/Katalog" ]; then
            echo "✅ Executable found and is executable"
          else
            echo "❌ Executable not found or not executable"
          fi
          
          # Check dependencies
          echo "Checking dependencies of main executable..."
          otool -L Katalog.app/Contents/MacOS/Katalog | head -10 || echo "Could not check dependencies"
          
          # Check bundle signature (informational)
          codesign -dv Katalog.app 2>&1 || echo "App not signed (expected for CI build)"
          
        else
          echo "❌ No app bundle found"
        fi

    - name: Upload DMG as Artifact
      uses: actions/upload-artifact@v4
      with:
        name: Katalog-macOS-v2.5-${{ env.VERSION_TAG }}
        path: build/Katalog-${{ env.VERSION_TAG }}-macOS.dmg
        retention-days: 30

    - name: Upload App Bundle as Artifact (for testing)
      uses: actions/upload-artifact@v4
      with:
        name: Katalog-macOS-AppBundle-v2.5-${{ env.VERSION_TAG }}
        path: build/Katalog.app
        retention-days: 7

    - name: Summary
      run: |
        echo "## macOS v2.5 Legacy Build Complete! 🍎" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "- **Version**: ${{ env.VERSION_TAG }}" >> $GITHUB_STEP_SUMMARY
        echo "- **File**: Katalog-${{ env.VERSION_TAG }}-macOS.dmg" >> $GITHUB_STEP_SUMMARY
        echo "- **Size**: $(ls -lh build/Katalog-*.dmg 2>/dev/null | awk '{print $5}' || echo 'N/A')" >> $GITHUB_STEP_SUMMARY
        echo "- **Target**: macOS 11.0+ (Big Sur and later)" >> $GITHUB_STEP_SUMMARY
        echo "- **Architecture**: $(uname -m)" >> $GITHUB_STEP_SUMMARY
        echo "- **Qt Version**: $($(brew --prefix qt@6)/bin/qmake -version | grep 'Qt version' || echo 'Unknown')" >> $GITHUB_STEP_SUMMARY
        echo "- **Codebase**: v2.5 (pure Qt6, no KDE dependencies)" >> $GITHUB_STEP_SUMMARY
        echo "- **Status**: 📦 Available as build artifact" >> $GITHUB_STEP_SUMMARY
        
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### Notes:" >> $GITHUB_STEP_SUMMARY
        echo "- Built from v2.5 tag (forced checkout)" >> $GITHUB_STEP_SUMMARY
        echo "- Uses pure Qt6 without KJob/KF dependencies" >> $GITHUB_STEP_SUMMARY
        echo "- Core catalog functionality from v2.5" >> $GITHUB_STEP_SUMMARY
        echo "- Compatible with Apple Silicon and Intel Macs" >> $GITHUB_STEP_SUMMARY
        echo "- May require 'Allow apps from anywhere' on first run" >> $GITHUB_STEP_SUMMARY
